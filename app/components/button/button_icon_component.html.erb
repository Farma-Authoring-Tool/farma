<% @actions.each do |action| %>
  <% icon = helpers.icon(action[:icon], class: action[:class]) %>

  <%= button_to action[:url], method: action[:method], data: { turbo_confirm: "Tem certeza?" } do %>
    <%= icon %>
  <% end %>
<% end %>
