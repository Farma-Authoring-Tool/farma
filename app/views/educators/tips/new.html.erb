<% provide :page_title, t(".page_title") %>
<% provide :page_heading, t(".page_heading",
                            lo_id: @lo.id,
                            exercise_id: @exercise.id,
                            solution_step_id: @solution_step.id) %>
<% provide :content_title, t(".content_title",
                             lo: @lo.title,
                             exercise: @exercise.title,
                             solution_step: @solution_step.title) %>

<%= render 'form' %>
