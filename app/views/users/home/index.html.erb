<body class="h-screen bg-zinc-900">
  <%= render 'users/home/partial/nav' %>

  <div class="bg-gradient-to-br text-white flex flex-col items-center justify-center gap-6 mt-10">
    <h1 class="text-2xl md:text-3xl font-semibold text-center mb-22 ">
      <%= t('user.select_user')%> 
    </h1>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-32 max-w-4xl w-full justify-center items-center">
      <%= rc "button/user", path:students_path, text:"Aluno", icon:"student"%> 
      <%= rc "button/user", path:educators_path, text:"Professor", icon:"educator"%> 
    </div>
  </div>
</body>