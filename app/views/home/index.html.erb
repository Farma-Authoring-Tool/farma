<div class="p-5">
   <h1>Home#index</h1>
   <p>Find me in app/views/home/index.html.erb</p>

   <br>

   <h2>Stimulus example</h2>

   <div data-controller="counter">
      <h2 data-counter-target="output">0</h2>
      <button data-action="click->counter#increment">+</button>
   </div>

   <br>

   <div data-controller="hello"></div>
   <br>

   <h2>Turbo example</h2>

   <%= turbo_frame_tag "test_id" do %>
      <p>Nothing, click the test to see the magic</p>

      <%= link_to 'Test', test_path %>
   <% end %>

   <br>

   <h2>Flash example</h2>

   <p><%= flash[:success] %></p>

   <br>

   <h2>Autenticado</h2>
   <% if current_user %>
      <p>Sim: <%= current_user.email %></p>
      <%= link_to 'Authenticate route', users_path %>
      <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete } %>
   <% else %>
      <p>Não</p>
      <%= link_to 'Login', new_user_session_path %>
   <% end %>

   <br>

</div>

<div class="border rounded-xl m-4 p-5">

   <h2>TailwindCSS Test</h2>

   <div class="p-4 mb-4 text-sm text-blue-800 rounded-lg bg-blue-50 dark:bg-gray-800 dark:text-blue-400" role="alert">
      <span class="font-medium">Info alert!</span> Change a few things up and try submitting again.
   </div>
   <div class="p-4 mb-4 text-sm text-red-800 rounded-lg bg-red-50 dark:bg-gray-800 dark:text-red-400" role="alert">
      <span class="font-medium">Danger alert!</span> Change a few things up and try submitting again.
   </div>
   <div class="p-4 mb-4 text-sm text-green-800 rounded-lg bg-green-50 dark:bg-gray-800 dark:text-green-400" role="alert">
      <span class="font-medium">Success alert!</span> Change a few things up and try submitting again.
   </div>
   <div class="p-4 mb-4 text-sm text-yellow-800 rounded-lg bg-yellow-50 dark:bg-gray-800 dark:text-yellow-300" role="alert">
      <span class="font-medium">Warning alert!</span> Change a few things up and try submitting again.
   </div>
   <div class="p-4 text-sm text-gray-800 rounded-lg bg-gray-50 dark:bg-gray-800 dark:text-gray-300" role="alert">
      <span class="font-medium">Dark alert!</span> Change a few things up and try submitting again.
   </div>
</div>
