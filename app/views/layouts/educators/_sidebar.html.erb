<aside data-sidebar-target="sidebar" class="fixed top-0 left-0 z-40 h-screen w-42 -translate-x-full pt-0 pt-16 transition-transform lg:translate-x-0">
  <div class="h-full overflow-y-auto bg-white shadow-lg px-3 py-4">

    <nav>
      <ul class="space-y-2 font-sm">
        <li class="text-green-700">
          <%= link_to educators_root_path do%>
            <%= icon(:home, class:"text-gray-400")%>
            <span class="ms-2 text-green-300">Dashboard</span>
          <% end %>

          <%= link_to educators_los_path do%>
            <%= icon(:oa, class:"text-gray-400")%>
            <span class="ms-2 text-green-300">Meus OAs</span>
          <% end %>

          <%= link_to educators_los_path do%>
            <%= icon(:class, class:"text-gray-400")%>
            <span class="ms-2 text-green-300">Turmas</span>
          <% end %>
        </li>
      </ul> 
    </nav>

  </div>
</aside>
