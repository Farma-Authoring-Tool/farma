<!DOCTYPE html>
<html>
<head>
  <%= render "layouts/shared/head" %>
  <%= tinymce_assets "data-turbo-track": "reload" %>
</head>

<body class="bg-gray-50">

<div data-controller="sidebar">
  <%= render 'layouts/educators/header' %>
  <%= render 'layouts/educators/sidebar' %>
</div>

<main class="h-screen px-4 py-24 lg:ml-42">

  <div class="mb-4 border-b border-gray-200 pb-2">
    <h1 class="text-2xl md:text-3xl font-bold text-gray-500"><%= yield(:page_heading) %></h1>
  </div>

  <%= render 'layouts/breadcrumbs/breadcrumbs' %>

  <div class="mx-auto mt-4 rounded-lg border border-gray-200 bg-white p-6">

    <%= render Flash::MessageComponent.new %>
    
    <% if content_for?(:content_title) %>
      <p class="text-xs font-semibold uppercase tracking-wide text-indigo-600 mb-5">
        <%= yield(:content_title) %>
      </p>
    <% end %>

    <%= yield %>

  </div>
</main>

<%= tinymce convert_urls: true, uploadimage: true %>
</body>
</html>
